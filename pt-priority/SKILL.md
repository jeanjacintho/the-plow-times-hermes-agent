---
name: pt-priority
description: Decide the owner's one #1 priority for today's paper from their prioritization file and calendar. Called by pt-research after the priority desk has gathered context. Never prints, never chats, never invents a priority the validator would refuse.
---

# pt-priority

The decision steps live here once the desk is wired. Until then this skill
only exists so the directory is a real skill in the image.
